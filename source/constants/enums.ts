export enum Method {
  GET = 'GET',
  HEAD = 'HEAD',
  POST = 'POST',
  PUT = 'PUT',
  PATCH = 'PATCH',
  DELETE = 'DELETE',
}

export enum MercureEventType {
  ROOM_UPDATED = 'ROOM_UPDATED',
  PLAYER_KILLED = 'PLAYER_KILLED',
}

export enum Locale {
  FRENCH = 'fr-FR',
  ENGLISH = 'en-US',
}
