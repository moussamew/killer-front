const { VITE_API_URL, VITE_MERCURE_URL, PROD, DEV } = import.meta.env;

export const PROD_ENV = PROD;
export const DEV_ENV = DEV;
export const API_URL = VITE_API_URL;
export const MERCURE_URL = VITE_MERCURE_URL;
